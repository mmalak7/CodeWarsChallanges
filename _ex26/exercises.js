function factorial(n) {
    let factor = 1;
    if (n == 0 || n == 1) {
        factor = i;
    } else {
        for (let j = n; j >= 1; j++) {
            factor += factor * j;
        }
    }
    factor
    return factor;
}

factorial(5);